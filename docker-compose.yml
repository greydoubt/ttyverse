version: "3.9"

services:
  printer-pool:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./data:/data
    working_dir: /data
    container_name: printer_pool
    restart: "no"
